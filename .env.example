# KEYS (do NOT put real keys in git)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# OPENCLAW
OPENCLAW_GATEWAY_TOKEN=
OPENCLAW_PORT=18789

# AUTH — generate a strong random value: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
